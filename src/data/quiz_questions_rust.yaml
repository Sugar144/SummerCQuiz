---
modules:
- number: 1
  explanation: "Módulo 1 Rust"
  levels:
  - number: 1
    explanation:
      Rust: |-
        ## ENUNCIADO

        Nivel 1 del módulo 1 de Rust. Repasa teoría de variables, condicionales y funciones con salida exacta.
    questions:
    - id: rust-m1-l1-q1
      language: Rust
      module: 1
      prompt: "[Rust] M1L1Q1: escribe código correcto para ejercicio básico 1."
      answer: |-
        use std::io::{self, Read};
        fn main(){ let mut s=String::new(); io::stdin().read_to_string(&mut s).unwrap(); let v:Vec<i32>=s.split_whitespace().map(|x|x.parse().unwrap()).collect(); println!("{}", v[0]+v[1]); }
      hint: "Cuida formato y sintaxis."
      mode: judge_rust
      tests:
      - input: |
          2 3
        output: |
          5
      - input: |
          10 -4
        output: |
          6
    - id: rust-m1-l1-q2
      language: Rust
      module: 1
      prompt: "[Rust] M1L1Q2: escribe código correcto para ejercicio básico 2."
      answer: |-
        use std::io::{self, Read};
        fn main(){ let mut s=String::new(); io::stdin().read_to_string(&mut s).unwrap(); let v:Vec<i32>=s.split_whitespace().map(|x|x.parse().unwrap()).collect(); println!("{}", v[0]+v[1]); }
      hint: "Cuida formato y sintaxis."
      mode: judge_rust
      tests:
      - input: |
          2 3
        output: |
          5
      - input: |
          10 -4
        output: |
          6
    - id: rust-m1-l1-q3
      language: Rust
      module: 1
      prompt: "[Rust] M1L1Q3: escribe código correcto para ejercicio básico 3."
      answer: |-
        use std::io::{self, Read};
        fn main(){ let mut s=String::new(); io::stdin().read_to_string(&mut s).unwrap(); let v:Vec<i32>=s.split_whitespace().map(|x|x.parse().unwrap()).collect(); println!("{}", v[0]+v[1]); }
      hint: "Cuida formato y sintaxis."
      mode: judge_rust
      tests:
      - input: |
          2 3
        output: |
          5
      - input: |
          10 -4
        output: |
          6
    - id: rust-m1-l1-q4
      language: Rust
      module: 1
      prompt: "[Rust] M1L1Q4: escribe código correcto para ejercicio básico 4."
      answer: |-
        use std::io::{self, Read};
        fn main(){ let mut s=String::new(); io::stdin().read_to_string(&mut s).unwrap(); let v:Vec<i32>=s.split_whitespace().map(|x|x.parse().unwrap()).collect(); println!("{}", v[0]+v[1]); }
      hint: "Cuida formato y sintaxis."
      mode: judge_rust
      tests:
      - input: |
          2 3
        output: |
          5
      - input: |
          10 -4
        output: |
          6
    - id: rust-m1-l1-q5
      language: Rust
      module: 1
      prompt: "[Rust] M1L1Q5: escribe código correcto para ejercicio básico 5."
      answer: |-
        // Rust normalize snippet 5
      hint: "Cuida formato y sintaxis."
      mode: normalize
  - number: 2
    explanation:
      Rust: |-
        ## ENUNCIADO

        Nivel 2 del módulo 1 de Rust. Repasa teoría de variables, condicionales y funciones con salida exacta.
    questions:
    - id: rust-m1-l2-q1
      language: Rust
      module: 1
      prompt: "[Rust] M1L2Q1: escribe código correcto para ejercicio básico 1."
      answer: |-
        use std::io::{self, Read};
        fn main(){ let mut s=String::new(); io::stdin().read_to_string(&mut s).unwrap(); let v:Vec<i32>=s.split_whitespace().map(|x|x.parse().unwrap()).collect(); println!("{}", v[0]+v[1]); }
      hint: "Cuida formato y sintaxis."
      mode: judge_rust
      tests:
      - input: |
          2 3
        output: |
          5
      - input: |
          10 -4
        output: |
          6
    - id: rust-m1-l2-q2
      language: Rust
      module: 1
      prompt: "[Rust] M1L2Q2: escribe código correcto para ejercicio básico 2."
      answer: |-
        use std::io::{self, Read};
        fn main(){ let mut s=String::new(); io::stdin().read_to_string(&mut s).unwrap(); let v:Vec<i32>=s.split_whitespace().map(|x|x.parse().unwrap()).collect(); println!("{}", v[0]+v[1]); }
      hint: "Cuida formato y sintaxis."
      mode: judge_rust
      tests:
      - input: |
          2 3
        output: |
          5
      - input: |
          10 -4
        output: |
          6
    - id: rust-m1-l2-q3
      language: Rust
      module: 1
      prompt: "[Rust] M1L2Q3: escribe código correcto para ejercicio básico 3."
      answer: |-
        use std::io::{self, Read};
        fn main(){ let mut s=String::new(); io::stdin().read_to_string(&mut s).unwrap(); let v:Vec<i32>=s.split_whitespace().map(|x|x.parse().unwrap()).collect(); println!("{}", v[0]+v[1]); }
      hint: "Cuida formato y sintaxis."
      mode: judge_rust
      tests:
      - input: |
          2 3
        output: |
          5
      - input: |
          10 -4
        output: |
          6
    - id: rust-m1-l2-q4
      language: Rust
      module: 1
      prompt: "[Rust] M1L2Q4: escribe código correcto para ejercicio básico 4."
      answer: |-
        use std::io::{self, Read};
        fn main(){ let mut s=String::new(); io::stdin().read_to_string(&mut s).unwrap(); let v:Vec<i32>=s.split_whitespace().map(|x|x.parse().unwrap()).collect(); println!("{}", v[0]+v[1]); }
      hint: "Cuida formato y sintaxis."
      mode: judge_rust
      tests:
      - input: |
          2 3
        output: |
          5
      - input: |
          10 -4
        output: |
          6
    - id: rust-m1-l2-q5
      language: Rust
      module: 1
      prompt: "[Rust] M1L2Q5: escribe código correcto para ejercicio básico 5."
      answer: |-
        // Rust normalize snippet 5
      hint: "Cuida formato y sintaxis."
      mode: normalize
  - number: 3
    explanation:
      Rust: |-
        ## ENUNCIADO

        Nivel 3 del módulo 1 de Rust. Repasa teoría de variables, condicionales y funciones con salida exacta.
    questions:
    - id: rust-m1-l3-q1
      language: Rust
      module: 1
      prompt: "[Rust] M1L3Q1: escribe código correcto para ejercicio básico 1."
      answer: |-
        use std::io::{self, Read};
        fn main(){ let mut s=String::new(); io::stdin().read_to_string(&mut s).unwrap(); let v:Vec<i32>=s.split_whitespace().map(|x|x.parse().unwrap()).collect(); println!("{}", v[0]+v[1]); }
      hint: "Cuida formato y sintaxis."
      mode: judge_rust
      tests:
      - input: |
          2 3
        output: |
          5
      - input: |
          10 -4
        output: |
          6
    - id: rust-m1-l3-q2
      language: Rust
      module: 1
      prompt: "[Rust] M1L3Q2: escribe código correcto para ejercicio básico 2."
      answer: |-
        use std::io::{self, Read};
        fn main(){ let mut s=String::new(); io::stdin().read_to_string(&mut s).unwrap(); let v:Vec<i32>=s.split_whitespace().map(|x|x.parse().unwrap()).collect(); println!("{}", v[0]+v[1]); }
      hint: "Cuida formato y sintaxis."
      mode: judge_rust
      tests:
      - input: |
          2 3
        output: |
          5
      - input: |
          10 -4
        output: |
          6
    - id: rust-m1-l3-q3
      language: Rust
      module: 1
      prompt: "[Rust] M1L3Q3: escribe código correcto para ejercicio básico 3."
      answer: |-
        use std::io::{self, Read};
        fn main(){ let mut s=String::new(); io::stdin().read_to_string(&mut s).unwrap(); let v:Vec<i32>=s.split_whitespace().map(|x|x.parse().unwrap()).collect(); println!("{}", v[0]+v[1]); }
      hint: "Cuida formato y sintaxis."
      mode: judge_rust
      tests:
      - input: |
          2 3
        output: |
          5
      - input: |
          10 -4
        output: |
          6
    - id: rust-m1-l3-q4
      language: Rust
      module: 1
      prompt: "[Rust] M1L3Q4: escribe código correcto para ejercicio básico 4."
      answer: |-
        use std::io::{self, Read};
        fn main(){ let mut s=String::new(); io::stdin().read_to_string(&mut s).unwrap(); let v:Vec<i32>=s.split_whitespace().map(|x|x.parse().unwrap()).collect(); println!("{}", v[0]+v[1]); }
      hint: "Cuida formato y sintaxis."
      mode: judge_rust
      tests:
      - input: |
          2 3
        output: |
          5
      - input: |
          10 -4
        output: |
          6
    - id: rust-m1-l3-q5
      language: Rust
      module: 1
      prompt: "[Rust] M1L3Q5: escribe código correcto para ejercicio básico 5."
      answer: |-
        // Rust normalize snippet 5
      hint: "Cuida formato y sintaxis."
      mode: normalize
- number: 2
  explanation: "Módulo 2 Rust"
  levels:
  - number: 1
    explanation:
      Rust: |-
        ## ENUNCIADO

        Nivel 1 del módulo 2 de Rust. Repasa teoría de variables, condicionales y funciones con salida exacta.
    questions:
    - id: rust-m2-l1-q1
      language: Rust
      module: 2
      prompt: "[Rust] M2L1Q1: escribe código correcto para ejercicio básico 1."
      answer: |-
        use std::io::{self, Read};
        fn main(){ let mut s=String::new(); io::stdin().read_to_string(&mut s).unwrap(); let v:Vec<i32>=s.split_whitespace().map(|x|x.parse().unwrap()).collect(); println!("{}", v[0]+v[1]); }
      hint: "Cuida formato y sintaxis."
      mode: judge_rust
      tests:
      - input: |
          2 3
        output: |
          5
      - input: |
          10 -4
        output: |
          6
    - id: rust-m2-l1-q2
      language: Rust
      module: 2
      prompt: "[Rust] M2L1Q2: escribe código correcto para ejercicio básico 2."
      answer: |-
        use std::io::{self, Read};
        fn main(){ let mut s=String::new(); io::stdin().read_to_string(&mut s).unwrap(); let v:Vec<i32>=s.split_whitespace().map(|x|x.parse().unwrap()).collect(); println!("{}", v[0]+v[1]); }
      hint: "Cuida formato y sintaxis."
      mode: judge_rust
      tests:
      - input: |
          2 3
        output: |
          5
      - input: |
          10 -4
        output: |
          6
    - id: rust-m2-l1-q3
      language: Rust
      module: 2
      prompt: "[Rust] M2L1Q3: escribe código correcto para ejercicio básico 3."
      answer: |-
        use std::io::{self, Read};
        fn main(){ let mut s=String::new(); io::stdin().read_to_string(&mut s).unwrap(); let v:Vec<i32>=s.split_whitespace().map(|x|x.parse().unwrap()).collect(); println!("{}", v[0]+v[1]); }
      hint: "Cuida formato y sintaxis."
      mode: judge_rust
      tests:
      - input: |
          2 3
        output: |
          5
      - input: |
          10 -4
        output: |
          6
    - id: rust-m2-l1-q4
      language: Rust
      module: 2
      prompt: "[Rust] M2L1Q4: escribe código correcto para ejercicio básico 4."
      answer: |-
        use std::io::{self, Read};
        fn main(){ let mut s=String::new(); io::stdin().read_to_string(&mut s).unwrap(); let v:Vec<i32>=s.split_whitespace().map(|x|x.parse().unwrap()).collect(); println!("{}", v[0]+v[1]); }
      hint: "Cuida formato y sintaxis."
      mode: judge_rust
      tests:
      - input: |
          2 3
        output: |
          5
      - input: |
          10 -4
        output: |
          6
    - id: rust-m2-l1-q5
      language: Rust
      module: 2
      prompt: "[Rust] M2L1Q5: escribe código correcto para ejercicio básico 5."
      answer: |-
        // Rust normalize snippet 5
      hint: "Cuida formato y sintaxis."
      mode: normalize
  - number: 2
    explanation:
      Rust: |-
        ## ENUNCIADO

        Nivel 2 del módulo 2 de Rust. Repasa teoría de variables, condicionales y funciones con salida exacta.
    questions:
    - id: rust-m2-l2-q1
      language: Rust
      module: 2
      prompt: "[Rust] M2L2Q1: escribe código correcto para ejercicio básico 1."
      answer: |-
        use std::io::{self, Read};
        fn main(){ let mut s=String::new(); io::stdin().read_to_string(&mut s).unwrap(); let v:Vec<i32>=s.split_whitespace().map(|x|x.parse().unwrap()).collect(); println!("{}", v[0]+v[1]); }
      hint: "Cuida formato y sintaxis."
      mode: judge_rust
      tests:
      - input: |
          2 3
        output: |
          5
      - input: |
          10 -4
        output: |
          6
    - id: rust-m2-l2-q2
      language: Rust
      module: 2
      prompt: "[Rust] M2L2Q2: escribe código correcto para ejercicio básico 2."
      answer: |-
        use std::io::{self, Read};
        fn main(){ let mut s=String::new(); io::stdin().read_to_string(&mut s).unwrap(); let v:Vec<i32>=s.split_whitespace().map(|x|x.parse().unwrap()).collect(); println!("{}", v[0]+v[1]); }
      hint: "Cuida formato y sintaxis."
      mode: judge_rust
      tests:
      - input: |
          2 3
        output: |
          5
      - input: |
          10 -4
        output: |
          6
    - id: rust-m2-l2-q3
      language: Rust
      module: 2
      prompt: "[Rust] M2L2Q3: escribe código correcto para ejercicio básico 3."
      answer: |-
        use std::io::{self, Read};
        fn main(){ let mut s=String::new(); io::stdin().read_to_string(&mut s).unwrap(); let v:Vec<i32>=s.split_whitespace().map(|x|x.parse().unwrap()).collect(); println!("{}", v[0]+v[1]); }
      hint: "Cuida formato y sintaxis."
      mode: judge_rust
      tests:
      - input: |
          2 3
        output: |
          5
      - input: |
          10 -4
        output: |
          6
    - id: rust-m2-l2-q4
      language: Rust
      module: 2
      prompt: "[Rust] M2L2Q4: escribe código correcto para ejercicio básico 4."
      answer: |-
        use std::io::{self, Read};
        fn main(){ let mut s=String::new(); io::stdin().read_to_string(&mut s).unwrap(); let v:Vec<i32>=s.split_whitespace().map(|x|x.parse().unwrap()).collect(); println!("{}", v[0]+v[1]); }
      hint: "Cuida formato y sintaxis."
      mode: judge_rust
      tests:
      - input: |
          2 3
        output: |
          5
      - input: |
          10 -4
        output: |
          6
    - id: rust-m2-l2-q5
      language: Rust
      module: 2
      prompt: "[Rust] M2L2Q5: escribe código correcto para ejercicio básico 5."
      answer: |-
        // Rust normalize snippet 5
      hint: "Cuida formato y sintaxis."
      mode: normalize
  - number: 3
    explanation:
      Rust: |-
        ## ENUNCIADO

        Nivel 3 del módulo 2 de Rust. Repasa teoría de variables, condicionales y funciones con salida exacta.
    questions:
    - id: rust-m2-l3-q1
      language: Rust
      module: 2
      prompt: "[Rust] M2L3Q1: escribe código correcto para ejercicio básico 1."
      answer: |-
        use std::io::{self, Read};
        fn main(){ let mut s=String::new(); io::stdin().read_to_string(&mut s).unwrap(); let v:Vec<i32>=s.split_whitespace().map(|x|x.parse().unwrap()).collect(); println!("{}", v[0]+v[1]); }
      hint: "Cuida formato y sintaxis."
      mode: judge_rust
      tests:
      - input: |
          2 3
        output: |
          5
      - input: |
          10 -4
        output: |
          6
    - id: rust-m2-l3-q2
      language: Rust
      module: 2
      prompt: "[Rust] M2L3Q2: escribe código correcto para ejercicio básico 2."
      answer: |-
        use std::io::{self, Read};
        fn main(){ let mut s=String::new(); io::stdin().read_to_string(&mut s).unwrap(); let v:Vec<i32>=s.split_whitespace().map(|x|x.parse().unwrap()).collect(); println!("{}", v[0]+v[1]); }
      hint: "Cuida formato y sintaxis."
      mode: judge_rust
      tests:
      - input: |
          2 3
        output: |
          5
      - input: |
          10 -4
        output: |
          6
    - id: rust-m2-l3-q3
      language: Rust
      module: 2
      prompt: "[Rust] M2L3Q3: escribe código correcto para ejercicio básico 3."
      answer: |-
        use std::io::{self, Read};
        fn main(){ let mut s=String::new(); io::stdin().read_to_string(&mut s).unwrap(); let v:Vec<i32>=s.split_whitespace().map(|x|x.parse().unwrap()).collect(); println!("{}", v[0]+v[1]); }
      hint: "Cuida formato y sintaxis."
      mode: judge_rust
      tests:
      - input: |
          2 3
        output: |
          5
      - input: |
          10 -4
        output: |
          6
    - id: rust-m2-l3-q4
      language: Rust
      module: 2
      prompt: "[Rust] M2L3Q4: escribe código correcto para ejercicio básico 4."
      answer: |-
        use std::io::{self, Read};
        fn main(){ let mut s=String::new(); io::stdin().read_to_string(&mut s).unwrap(); let v:Vec<i32>=s.split_whitespace().map(|x|x.parse().unwrap()).collect(); println!("{}", v[0]+v[1]); }
      hint: "Cuida formato y sintaxis."
      mode: judge_rust
      tests:
      - input: |
          2 3
        output: |
          5
      - input: |
          10 -4
        output: |
          6
    - id: rust-m2-l3-q5
      language: Rust
      module: 2
      prompt: "[Rust] M2L3Q5: escribe código correcto para ejercicio básico 5."
      answer: |-
        // Rust normalize snippet 5
      hint: "Cuida formato y sintaxis."
      mode: normalize
